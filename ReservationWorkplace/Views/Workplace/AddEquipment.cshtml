@model ReservationWorkplace.Models.EquipmentForWorkplaceViewModel

@{
    ViewData["Title"] = "Create EquipmentForWorkplace";
}

<h1>@ViewData["Title"]</h1>
<hr />
<div class="row">
    <div class="col-md-4">
        <div asp-validation-summary="ModelOnly"></div>
        <form asp-action="AddEquipment">
            <div class="form-group">
                <label asp-for="EquipmentId" class="control-label">Equipment</label>
                <select asp-for="EquipmentId" class="form-control" asp-items="ViewBag.Equipments">
                    <option value="" hidden>Select Equipment</option>
                </select>
                <span asp-validation-for="EquipmentId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="WorkplaceId" class="control-label">Workplace</label>
                <select asp-for="WorkplaceId" class="form-control" asp-items="ViewBag.Workplaces">
                    <option value="" hidden>--Select Workplace--</option>
                </select>
                <span asp-validation-for="WorkplaceId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Count" class="control-label">Count</label>
                <input asp-for="Count" class="form-control" />
                <span asp-validation-for="Count" class="text-danger"></span>
            </div>
            <br />
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-success" />
            </div>
        </form>
    </div>
</div>
<br />
<div>
    <a asp-action="Index" class="btn btn-primary">Back to List</a>
</div>